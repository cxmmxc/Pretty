<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppBaseTheme.Dark">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppBaseTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:fitsSystemWindows">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@color/text_color_primary</item>
        <item name="android:textColorSecondary">@color/text_second_color_primary</item>
        <item name="android:windowBackground">@color/window_background</item>

        <item name="android:activatedBackgroundIndicator">@drawable/selector_draw_listitem</item>
        <item name="selectableItemBackground">@drawable/selector_draw_listitem</item>
    </style>

    <style name="AppBaseTheme.Dark" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="android:fitsSystemWindows">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@color/text_color_primary</item>
        <item name="android:textColorSecondary">@color/text_second_color_primary</item>
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:activatedBackgroundIndicator">@drawable/selector_draw_listitem</item>
        <item name="selectableItemBackground">@drawable/selector_draw_listitem</item>
    </style>

    <style name="style_loadingdialog" parent="@android:style/Theme.Dialog">
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">false</item>
        <!-- 是否半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否显示title -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置dialog的背景 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景是否模糊显示 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 动画效果 -->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:indeterminateDrawable">@drawable/progressbar_large</item>
    </style>

    <style name="text_color_style" >
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="ToolbarTheme" parent="AppBaseTheme">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="colorControlActivated">@color/title_bg</item>
    </style>

    <style name="BgTheme" parent="AppBaseTheme.Dark">
        <item name="android:background">@color/title_bg</item>
        <item name="colorPrimary">@color/title_bg</item>
        <item name="colorPrimaryDark">@color/title_bg</item>
        <item name="colorAccent">@color/title_bg</item>
    </style>
</resources>
